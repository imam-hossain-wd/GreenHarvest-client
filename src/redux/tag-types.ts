export enum tagTypes {
    user = "user",
    product = "product",
    order = "order",
    payment = "payment",
  }
  
  export const tagTypesList = [
    tagTypes.user,
    tagTypes.product,
    tagTypes.order,
    tagTypes.payment,
  ];